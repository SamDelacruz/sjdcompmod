#include <iostream>
#include <cfloat>

int main(){

	float f[3];
	f[0] = 1.0f;
	f[1] = 2.0f;
	f[2] = 4.0f;

	while(f[2] != f[1]){

		f[2] *= 2.0f;
		f[1] *= 2.0f;
		f[0] *= 2.0f;
	}
	
	std::cout<<"Max value for f = "<<f[0]<<std::endl;
	std::cout<<"FLT_MAX = "<<FLT_MAX<<std::endl;	
	return 0;

}
